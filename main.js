const hexToBrandTokens = require('./config/hex-to-brand-tokens.json');
const hexToPaletteTokens = require('./config/hex-to-palette-tokens.json');
const palettes = require('./config/palettes.json');
const webcolors = require('./config/web-colors.json');

module.exports = {
  hexToBrandTokens,
  hexToPaletteTokens,
  palettes,
  webcolors,
};
